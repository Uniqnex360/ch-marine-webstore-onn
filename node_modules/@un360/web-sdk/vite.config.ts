import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { resolve } from 'path'

export default defineConfig({
  plugins: [react()],
  build: {
    lib: {
      entry: resolve(__dirname, 'src/index.ts'),
      name: 'ProductChatbotWebSDK',
      fileName: (format) => `un360-web-sdk.${format}.js`
    },
    rollupOptions: {
      external: ['react', 'react-dom', '@un360/core', '@un360/ui'],
      output: {
        globals: {
          react: 'React',
          'react-dom': 'ReactDOM'
        }
      }
    }
  },
  server: {
    port: 3000,
    open: true
  }
})